The <strong><code>tabindex</code></strong> <a href="/en-US/docs/Web/HTML/Global_attributes">global attribute</a> indicates if its element can be focused, and if/where it participates in sequential keyboard navigation (usually with the <kbd>Tab</kbd> key, hence the name).